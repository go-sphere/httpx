# BENCHMARK.md is generated by `make bench-report` (or re-rendered from samples
# already on disk with `python3 benchmarks/report.py --render-only`). Marking it
# generated collapses it by default in diffs and pull requests and keeps it out
# of the repository's language statistics -- edit the generator, not the file.
#
# The raw samples it is rendered from are not tracked at all; see .gitignore.
benchmarks/BENCHMARK.md   linguist-generated=true
